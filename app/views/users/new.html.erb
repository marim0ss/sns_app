<h1>ユーザー新規登録</h1>

<%= form_for @user, url: "/users" , html: { method: :post } do |f| %>
  <p>メールアドレス：<%= f.text_field :email %></p>
  <p>パスワード：<%= f.text_field :password %></p>
  <p>パスワード確認用：<%= f.text_field :password_confirmation %></p>
  <input type="submit" value="新規登録">
<% end %>
